<?php

use A\B\Replaced;

class Sample extends Replaced implements Replaced
{
    public function __construct(Replaced $class)
    {
        try {
            if ($class instanceof Replaced) {
                Replaced\func('arg');
            }
        } catch (Replaced $e) {
            throw new Replaced();
        }
    }
}
